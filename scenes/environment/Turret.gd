extends Object_Interactable

func overlap(object : Node2D) -> void:
	print("killed " + str(object))
	
# TODO making turrets work
# TODO making turrets detect a target
# TODO making turrets shoot only one person at a time
